/*
|--------------------------------------------------------------------------
| mutation (同步操作)
|--------------------------------------------------------------------------
|
*/

// this => store

export default {
    'CHANGE_LANGUAGE' (state, payload) {
        state.language = payload
    }
}
